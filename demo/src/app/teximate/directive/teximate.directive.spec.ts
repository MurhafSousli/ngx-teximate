import { LetteringDirective } from './lettering.directive';

describe('LetteringDirective', () => {
  it('should create an instance', () => {
    const directive = new LetteringDirective();
    expect(directive).toBeTruthy();
  });
});
